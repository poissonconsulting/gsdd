  name: fledge-bump
  on:
    schedule:
      - cron: "0 7 * * *"
    workflow_dispatch:
  permissions:
    contents: write
    pull-requests: write
  jobs:
    fledge-bump:
      uses: poissonconsulting/.github/.github/workflows/fledge-bump.yml@f-fledge-automation@v1
      secrets: inherit
